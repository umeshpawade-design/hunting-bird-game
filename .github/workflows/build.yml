name: Build APK from Nested Tarball

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y git zip unzip openjdk-17-jdk autoconf automake libtool pkg-config zlib1g-dev libncurses5-dev libsqlite3-dev libgdbm-dev libssl-dev libffi-dev libbz2-dev libreadline-dev
        pip install --upgrade pip
        pip install "buildozer>=1.5.0" "cython<3.0.0"

    - name: Extract and Clean Project Files
      run: |
        if [ -f ".github/workflows/ReplitExport-umeshpawade007.tar.gz" ]; then
          cp .github/workflows/ReplitExport-umeshpawade007.tar.gz .
        fi
        
        tar -xzvf ReplitExport-umeshpawade007.tar.gz
        
        if [ -d "umeshpawade007" ]; then
          mv umeshpawade007/* .
        elif [ -d "hunting-bird-game" ]; then
          mv hunting-bird-game/* .
        fi
        
        rm -rf .buildozer buildozer.spec bin
        ls -la

    - name: Build with Buildozer
      run: |
        buildozer init
        
        # Application Config Injection
        sed -i 's/^title =.*/title = Hunting Bird/' buildozer.spec
        sed -i 's/^package.name =.*/package.name = huntingbird/' buildozer.spec
        sed -i 's/^package.domain =.*/package.domain = org.jmd/' buildozer.spec
        sed -i 's/^source.include_exts =.*/source.include_exts = py,png,jpg,jpeg,kv,atlas,mp3,wav/' buildozer.spec
        
        # SDK/NDK and License settings
        sed -i 's/^#\? \?android.api =.*/android.api = 33/' buildozer.spec
        sed -i 's/^#\? \?android.ndk =.*/android.ndk = 25b/' buildozer.spec
        sed -i 's/^#\? \?android.accept_sdk_license =.*/android.accept_sdk_license = True/' buildozer.spec
        
        # CRITICAL REFRESH: Undefined macro (libtoolize) issue ko fix karne ke liye
        export PATH="/usr/bin:/usr/local/bin:$PATH"
        
        # Final build execution
        buildozer android debug
